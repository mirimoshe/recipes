import { DifficultyFormatterPipePipe } from './difficulty-formatter-pipe.pipe';

describe('DifficultyFormatterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DifficultyFormatterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
